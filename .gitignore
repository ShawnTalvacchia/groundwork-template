# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts
tsconfig.tsbuildinfo

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo

# agent worktrees (live inside the repo; keep .claude/launch.json tracked)
.claude/worktrees/

# per-machine agent permissions — yours, not the project's
.claude/settings.local.json

# verification build dir (npm run verify) — never collides with dev
.next-verify/
.env*

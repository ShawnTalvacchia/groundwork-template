---
category: implementation
status: active
tier: working
last-reviewed: YYYY-MM-DD
read-when: "/system surface changes — new page, IA change, doc-format change"
---

# The System Surface (`/system`)

## What this is

`/system` is the project's knowledge system rendered as a reference layer inside the app — noindexed and absent from the product nav. It exists so the state of the work is legible at a glance while the docs stay the single source of truth.

Because the site deploys from pushes, the surface updates with every commit: push → rebuild → the pages re-read `docs/`.

**Two audiences, one repo.** The product is public and indexed; `/system` is the team's record. That split is enforced in code by the gate below, not left to the adopter.

> **This doc is the spec; the code is in the repo.** The dashboard ships with the template — `lib/system.ts`, `lib/derivation.ts`, `lib/styleguide.ts`, `app/system/*`, and the starter `app/globals.css`. Run it with `npm run dev` and open `/system`. This spec is the contract the parsed docs uphold, and the parser markers in those docs (`<!-- PARSED by … -->`) point back here.

## The gate

`/system` is gated by `proxy.ts` at the edge. Four states:

| Environment | `/system` | Why |
|---|---|---|
| development | open | The gate must never be local setup friction. |
| `SYSTEM_GATE=off` | open | Deliberately public. The choice is explicit and on the record. |
| `SYSTEM_PASSWORD=…` | password, then cookie | The default for a deployed project. |
| production, neither set | **blocked (503)** | Nobody decided; the safe reading of that is "don't publish the record." |

The last row is the design. A deploy that forgets to configure the gate fails closed and names both variables, so **public-by-choice replaces public-by-forgetting**. `robots: noindex` still ships, but it is a search-engine hint, never access control — the gate is what makes the record private.

The cookie value is a hash of the secret, so rotating `SYSTEM_PASSWORD` invalidates every session without a session store. The product is never gated; the matcher covers `/system` only.

**Alternatives, and when to prefer them:**

| Mechanism | Prefer it when | Cost |
|---|---|---|
| Shipped gate (`SYSTEM_PASSWORD`) | Default. Solo or small team, one shared secret, any host. | One env var. No accounts, no audit trail. |
| Host password protection (Vercel, Cloudflare Access) | Your host offers it and you want the gate above the app entirely. | Platform lock-in; often a paid tier. |
| Identity provider (SSO, OAuth) | Real teams, per-person access, revocation, an audit trail. | A dependency and a login flow to maintain. |
| Second private deployment | The record must never share an origin with the product. | Two deploys of one repo to keep in sync. |

Set `SYSTEM_GATE=off` only for a deliberately public dashboard — a demo, a template, or a project whose record is meant to be read.

## The law: derived, never authored

Every page renders from the markdown in `docs/` at build time — frontmatter, statuses, tiers, list items. Nothing on the surface is hand-maintained; **to change a page, change its source doc.** A visual layer that can drift is a second thing to review, which is the exact failure this system exists to prevent. If the surface disagrees with the docs, the docs win — the build is stale.

The parsers adapt to the docs' existing formats. The formats never bend to the parsers — the identifier schemes (`P##`, `§N`, `FC##`, the mode headings) are load-bearing across docs, code comments, and memory.

**The law covers vocabulary, not just values.** A page may not author the project's own words — the set of areas, their labels, their order, or a thesis about how they relate. Rendering a *value* the docs supplied is derivation; supplying the *category* it goes in is authorship, and it is the harder failure to see, because a label reads as chrome rather than as content. The Features page broke this from the beginning: its area set was hardcoded from one product's own thesis, so every project built from this template inherited that vocabulary and any feature outside it rendered nowhere at all. Where a page needs a category, it derives the category (`getFeatureAreas`) or formats the doc's own word (`areaLabel`). Section *headings* name the section, never a project's shape — this is why the roadmap's horizon section is "On the horizon."

## Shape: hub and spoke

A fixed header carries the main tabs — **System** + three groups (Work · Structure · Method) as segmented pills. Each group's pages render as subtabs in the body. The header and subtab row stick as one block; the body scrolls under.

**Tabs vs drill-ins.** A subtab is for a surface worth *exploring*; anything you merely *consult* is a card on the group's overview with the full page one click away. **Cap: four subtabs per group.** A tab never ejects you from the surface — bring content *in* rather than linking out.

## The three groups

- **Work** — *what's being done and what's waiting*: every open board, badged by mode and labelled with its stage — the active board per mode standing, the waiting ones set back, a run's boards grouped under the run board (`BoardCards`) — the **queue shelf** beneath it, and the three trackers. The shelf shows the queue rather than counting it — a tile whose only content was a number said less in the same space, and it never named a single phase. It is labelled `ROADMAP`, since that is the page it is a window onto, and carries the **full** queue count beside the label: the cards cap at four, so the count is what stops a capped list from reading as the whole list. A project with an empty queue still gets the shelf, because that is where the roadmap link lives.
- **Structure** — the shape of the project: the feature registry, the strategy shelf, the docs index (by tier + freshness), the shipped timeline, the decisions log, and the styleguide.
- **Method** — *how we work*, rendered rather than linked: the phase arc, the modes with their full rituals, the trackers model, the tier physics, the molds every board and seed is cut from, and the glossary. The rulebook docs (CONTRIBUTING, CLAUDE.md) appear here only as source notes at the foot of the page they back — they are docs like any other, and the docs index under Structure is where they are listed.

## Page → source mapping

| Page | Renders from |
|------|--------------|
| Overview | counts from every list + the Glossary + `ROADMAP.md` Where We Are + doc freshness + the **session-starters strip** (`StartersStrip`, inside Work above the active board: a shelf collapsed by default, each row a collapsed card — arrival → shape pill + mode, the same pairing the method page uses — expanding in place to its example prompt and what-happens text; the header's "How we work →" is the only link out) + the **queue shelf**: the next 4 queued rows joined to their seeds, mode-badged, each linking to its seed, under the full queue count (`QueueShelf`, shared with Work → Overview) |
| Active board(s) | `docs/phases/*.md` (templates excluded) — `/system/phase`. **With exactly one board open it redirects to that board's page** (a real 307, baked at build); with more than one it is **the index of open boards, one entry each**. The URL means "the active board," and one open board is the ordinary state, so an index of one card would be a click carrying no information — the index earns itself when there is something to choose between, which is a run or two modes open at once. With none it renders the queue under `Between boards`. The redirect is why a board page's breadcrumb parent is **Work's overview** and not this route: pointed here it would bounce the reader back to the board they just left. It renders `BoardCards` over `groupBoards`' groups, the same component the hub and Work render, plus a title and the open count: the order of a group and the shape of one are claims the parser already makes, and a second surface may not restate them differently — a second overview drawn here would be exactly that. Order is `groupBoards`': by mode — product · system · side · queue-shaping, the Levels line's and the starters' order — the group holding the active board first inside a mode; a **run** is the boards sharing a `run:`, led by the **run board** (the board whose name is the run's name), its members ordered by status then by the mode's kind sequence (`STATUS_RANK` · `MODE_KINDS`). Consecutive standalone groups collect into one grid so two small tiles still pair, a run breaks the row, and the next tile starts a new grid; nothing re-partitions the groups, because a card's reader has to meet the boards in the order the card listed them. A standalone board is one tile, a run is a shelf (`RunShelf`) headed by `RunHeader`, then the members in a grid — every member tile labelled `Active \| Waiting \| Paused · mode` (`STATUS_LABEL`) with the stage as its pill. Every tile links to **that board's own page**. **A run with no active member says `none active`** rather than dropping the clause: the run board can release the mode's active slot before any member takes it, and muting is relative — a shelf of uniformly faded tiles with nothing to compare against reads as a styling accident, not as a fact about the run. **The three statuses are ONE SCALE in one hue at two strengths**, which is what makes the rank unable to invert: `active` wears a 3px left bar in `--brand-main` (`.sys-tile-active`), `paused` the same bar in `--brand-faded` (`.sys-tile-paused`, and on a shelf it comes up to the top surface with the active board rather than fading with the rest), `waiting` no bar at all plus the inset surface (`.sys-tile-waiting`). A paused board also ranks *second*, above every waiting board (`STATUS_RANK`: active · paused · waiting), because `waiting` is a claim of completion and a board that stopped mid-kind must not render as one that finished. **The bar is a board's only status mark** — the stage pill is the ordinary chip on every board but the active one, since each callsite already prints the status in words beside it and a second mark was the claim stated twice. The scale is built from one token deliberately: a second hue ranks differently in each theme and does not survive a re-skin, which is why `--brand-faded` is a computed fade of whatever `--brand-main` is set to. **Hover lifts a card inside its own register and never restates its status:** full brand for the active board and for every tile that is not a board, `--brand-faded` for a paused one, a neutral step for a waiting one — carried on a shelf-scoped selector too, because `.sys-tile:hover` and `.sys-run .sys-tile-waiting` have equal specificity and source order alone once made one state behave two ways. The run's count line names it (`· N paused`) beside `none active`. **It fires no drift alarm**, and that is the call, not an omission: `paused` is a legal state rather than a record defect, so an alarm would fail `npm run verify` on a board the project deliberately stopped. Loudness here is rank, label and the bar; the only status that alarms is one the parser cannot read at all. |
| A board | `docs/phases/<slug>.md` (`/system/phase/<slug>`) — **one board, in full: this is its home.** The badge row (mode badge, **stage** pill brand-filled when the board is active, **status**, progress), the walkthrough callout, the section index and the body, then a link to the file in the doc reader. The three declared values come from the board's own frontmatter (`status: active \| waiting`, `stage:`, `run:` — CONTRIBUTING § Frontmatter maintenance), read by `getActiveBoards` and never inferred; a board with no `status` reads as active, so boards written before the field existed render as they did. **A member of a run carries `RunHeader` above it** — the same header the shelf uses, because the shape of a board is the same class of claim as the order of one — with the run's name linking to the run board's page. The name is plain text on the run board's *own* page, where the reader is already there. The header carries **no thesis line**: a sentence restated on every surface the header renders on is the duplication one-home-many-references exists to prevent — the reference is the name, and it links to the board that holds it. A member's badge row carries **no `run ·` clause** — the header above it has already said so. **Heading ids are unprefixed**, like the docs viewer's: one document per page cannot collide, so a board's `#items` is the same address on its page and in the doc reader. Breadcrumb `Overview › <slug>` under Work — the parent every hidden page in the group returns to, and loop-free where `/phase` would redirect straight back; the slug is verbatim the way a doc page shows its filename stem, since `nav.tsx` is a client component and cannot read the board's h1. **`generateStaticParams` yields a sentinel (`__none`) for a project between boards**, which the page `notFound()`s: an empty result prerenders the route for nothing at all (`slug-params.ts` → `NO_SLUGS`, shared with the walkthrough route). Heavy boards derive progress from each `## Workstream` section's Status-cell task rows + legacy checkboxes; light boards (no workstreams) count their `## Items` checkboxes directly, excluding the closing checklist |
| Roadmap | `ROADMAP.md` (Where We Are, the queue table — any mode, Key Considerations, horizon) + `planning/queued/*.md` seeds (`getQueuedSeeds` — **the card's sentence is the ROADMAP row's own Goal cell**; the seed supplies only frontmatter, `mode:` badging each, plus `priority`, `queued` and a note count, and `run:` groups them — rows sharing a run sit together (`groupQueue`: the first keeps the ROADMAP's position, the rest follow it) and each carries the run's name; the queue shelf groups the same way; the whole card links to its seed; the invariant is bidirectional and mode-agnostic — an orphaned seed or a seedless row trips the drift banner). **Sections render in the doc's own order** (`roadmap.sectionOrder`) — reorder the doc and the page follows. **The page opens with the open boards, derived** (`BoardCards` over `groupBoards(getActiveBoards())`, the same cards the hub and Work render), ahead of every doc section, because what is being worked on comes before what is queued. They are not a doc section, so they sit outside `sectionOrder`. A run renders as its shelf, members under the run board; no open board renders the `Between boards` tile |
| Questions | `planning/Open Questions & Assumptions Log.md` (`## N. Topic` → `### Question?` with Area/Opened/Priority/Thinking/Resolves-when; everything in the file is open — resolved is deleted) |
| Punch list | `planning/punch-list.md` (the `P##` table). The summary line is the row's own `title` column — the page used to guess it from a bold prefix or a truncated first sentence, which is the surface authoring a label the doc never wrote |
| Future | `planning/Future Considerations.md` (`FC##` sections, Trigger/Context/Effort fields) |
| Docs | `tier:` frontmatter across live docs + per-tier staleness heuristics (working >30d, commitments >90d). **Each tier explains itself where you meet it:** an `InsetNote` under every tier heading carries the tier table's other three columns — what lives there, what it takes to change, when it is re-checked, the staleness clock — which this page parsed and never rendered. `getTierPhysics`'s cross-cutting paragraphs (sinking, structured challenge, no clock, read-is-not-review) fold into one collapsed row at the top beside a link to `/system/tiers`. The page authors nothing; it had been printing bedrock's "Structured challenge — logged whether it succeeds or fails" as a bare subtitle with no route to what that means. The inset visual is `.sys-inset`, shared with the doc renderer's mold blockquote. The **briefing** (`CLAUDE.md`, at the project root rather than under `docs/`) is one of those docs — `getAllDocs` appends it when the project has one, and the row is labelled `project root`. It reached the surface no other way: `/system/docs/CLAUDE.md` rendered from the start and nothing linked it |
| Features | `docs/features/*.md` frontmatter (`feature-status`, `feature-kind`, `area`, `routes`). The area **set and labels** derive too (`getFeatureAreas` — alphabetical, title-cased); the page holds no vocabulary of its own |
| Strategy | `docs/strategy/**` frontmatter (`summary` one-liners; grouped by tier + subfolder) |
| Site | the **routes directory** — `app/` beside `docs/` (`ROUTES_DIR`, `lib/system.ts`), read by `getSiteMap`: file-based routing, one entry per `page.*` file, `route.*` files marked as handlers, dynamic segments shown in their brackets, `(group)` folders folded into their parent, `_`/`@`/dot folders skipped, a folder with nothing routable under it not a node. The one page here whose source is code rather than a doc, derived for the same reason as the rest: a run's picture holds the **aspirational** map (`planning/<run>-picture.md`), and its close reconciles that against this, the map that exists (CONTRIBUTING § The phase pipeline). Where an open product board carries a filled `## Survey` table (`getSurveyRows` — the first column names the surface, the rest are kept as the board's own headers), each page route is matched to the rows by path (`routeCovered`): a route no row names carries a `no survey row` pill, an inset counts them, and the row's cells sit on the route it names directly — a descendant covered by prefix is covered silently. A static page route links to itself. **The page links the picture it names**: an open board's `**Picture:**` line (`ActivePhase.picture`, the first `.md` link, resolved from `phases/`) where a run is open, else the picture mold. **Hidden under Structure**, offered from the overview in one line with the page count: it is reached for at one moment — a run's survey or close — and the drift banner links here when a route falls outside the table |
| Timeline | `docs/archive/phases/*.md` (walkthroughs skipped) — month-grouped, newest first |
| Decisions | `docs/decisions.md` (`## date · title` entries, What/Why/Instead of/Scope) |
| Method → How we work | `CONTRIBUTING.md § The Work Model` (`getWorkModel`) + `§ The phase pipeline` (`getPhasePipeline`). The page teaches the flow first and folds the reference: **role cards → pipeline → modes → starters → shared rules → the kit**, regardless of doc order. The role cards carry the arc: one card per pipeline role bullet — role name + level pill — tagged with the steps it runs, the tags derived from the Work Model lede's own "its arc is …" sentence (`WorkModel.arc`). The tags render only for the canonical shape (four steps, three roles, the middle role running build + review); other counts render untagged, because the join is layout, not doc content. § The phase pipeline renders as `#phase-pipeline` — lede expanded, its trailing bold-led rules as collapsed rows; parsed "§ The phase pipeline" references become in-page anchor links via `MdInline`'s opt-in anchors map (unmapped § names stay plain text, deliberately). A canon with no pipeline section renders no role cards — absence is legitimate, and the arc still reads in the lede's prose. The modes render next — rituals stay folded, labeled as built-in and self-running, their step headers carrying role pills (opening = the pipeline's first role, during = the second, closing = the third; rendered only when the three-role join holds); session starters reuse the hub's collapsed rows (`StarterRows`, shared with `StartersStrip`); each shared rule folds to its bold lead (a rule with no bold lead renders as a plain row); § The parts and § Adjustments demote to collapsed shelves at the foot |
| Method → Trackers | `CONTRIBUTING.md § The Planning Trackers` |
| Method → Tiers | `CONTRIBUTING.md § Doc Tiers & Review Physics` |
| Method → Molds | every `_`-prefixed `.md` under the docs tree (`getMolds`) — the board, walkthrough and seed templates. The set **derives**: a mold is exactly what the registry's `walk` skips, so a project that adds one gets it rendered and this page enumerates nothing. Per mold the page shows the h1, the `mode:` badge where the field names one mode, **two field lists** — the frontmatter block, then the bold-led `**Label:**` lines between the h1 and the first `##` — each with the placeholder the mold offers, in the mold's own order, plus the bold label of each inset card, the `##` sections, and a link into the doc reader for the file itself. The lists are labelled by where they sit (*in the frontmatter* · *on the page*), not by syntax: the page is read by someone asking what a mold will ask them for. Which parser reads a field is deliberately **not** shown — that is code knowledge, and a hand-kept map of it would be the surface authoring what the docs never said. The page exists because a mold is only ever met as a file you are already filling in, so a field the mold omits is invisible by construction; and because the molds were unreachable, `walk` skipping them so nothing indexed them and only the doc reader rendered them, at a URL nothing offered. Read-only by design |
| Method → Glossary | `CONTRIBUTING.md § Glossary` |
| `/system/docs/<path>` | any single doc rendered whole (archive + `_`-prefixed templates included, plus the briefing at the project root — `getAllDocPaths` supplies the routes, so a project with no briefing prerenders none). The header card carries four values as chrome — tier, status, freshness, `read-when` — and, folded under them, **every frontmatter key the file declares, in its own order** (`getDocByPath`'s `frontmatter`). That set of four does not grow when a doc declares more, so a seed's `priority`, a board's `mode` and a feature's `area` reached no reader on the one page that shows the file itself; the fold is derived whole, so a doc that invents a key shows it. Relative `.md` links resolve in place, `#fragments` land on GitHub-style heading ids stamped on h2–h4 |
| Styleguide | `app/globals.css`, parsed by `lib/styleguide.ts` — light + dark side by side, token health checks per build |

**A `mermaid` fence in any rendered doc is a diagram** (`Mermaid`, `app/system/mermaid.tsx`, hooked into `DocProse`'s `pre`). The run's picture doc exists so a person can *see* the loop and the site the run is building toward, and a flow map read as source is not a picture. Rendered on the client (mermaid is a DOM library), **skinned from the tokens at render time** — it reads the same variables the page does, so it holds in dark mode and follows the toggle live — with the doc's own source folded under it, one click away. A fence that fails to parse shows the error and the source rather than nothing; every other fence stays a code block.

## Drift alarms

**The doc formats are parser API, and format drift fails silently** — a parser handed an unexpected shape returns empty or partial, and the page renders hollow with no signal. Three defenses:

- **Invariants, not zero-checks** — `lib/derivation.ts` asserts each parser's promised shape (every mode heading parsed with all fields — the count is the mount's own, at least 3 (`WorkModel.modeHeadings`) — 5 parts with Is/Properties, 5+ session-starter rows with no empty cell, 3+ adjustments, the known tiers with live thresholds (asserted against `TIER_ORDER`, not a hard-coded count), every question carrying area + resolves-when, frontmatter coverage …). Non-zero-but-wrong parses are the worst class; zero-checks alone miss them.
- **Unrendered features** — every product feature doc must land in a bucket the Features page renders. `getFeatureAreas` derives the area set, so total coverage holds by construction; the alarm fires if a fixed vocabulary is reintroduced, because a fixed set drops the docs that fall outside it. Its limit is stated in the code — it proves no doc was dropped, not that the labels came from the docs; nothing in `lib/` can see a literal authored into a page under `app/`.
- **Dangling references** — every tracker ID named in the two state docs (`ROADMAP.md` and the project root's `CLAUDE.md`) must still exist in its tracker: `P##` in the punch list, `FC##` in Future Considerations, `§N` in the Open Questions log. Parsed by `getStateReferences`; prose only, one alarm per dangling ID per doc. This is the one invariant that guards *content* rather than format, and it earns the exception by catching the failure format checks cannot see: a doc still describing an item as pending after the item shipped and left its tracker. `§ N` with a space is a section pointer, not an ID, and is not scanned.
- **A page route the survey never named** — where the routes directory exists *and* an open product board carries a filled `## Survey` table, every page route (`page.*`, never a handler) must be covered by a row's path in the Surface cell (`routeCovered`: equal, or a whole-segment prefix, a dynamic segment matching any one). One alarm per uncovered route, naming the page file. Presence-not-count twice over: no routes directory, or no filled table, is silent — a project that runs no runs never sees it. The table is the run's claim to have weighed every surface, and a route outside it is a surface built outside the run's picture or added after the survey ran. A row written in words alone covers nothing; the alarm says to name the path.
- **The open boards' declared fields** — a `status` that is neither `active` nor `waiting` (the board renders as active, silently), two active boards in one mode (the concurrency rule; the surface would mark both), or a declared `stage` that is not one of the mode's kinds (`MODE_KINDS`; a run's members order by it, and an unknown one sorts last with no signal). Presence-not-count: a board declaring none of the three fires nothing — it renders as it did before the fields existed. Queue-shaping's one kind has no name, so its stage is never checked.
- **A phase pipeline that parses hollow** — § The phase pipeline is presence-not-count: a canon without the section is silent (a project may reshape the model away from split phases), but a section that exists while its lede or role bullets parse empty fires, because the method page would render its heading over nothing. No role-count assertion — the roles are the project's own words.
- **A mold that parsed hollow** — presence-not-count, with no floor: a project with no molds is legitimate. What fires is a mold with no h1 or no frontmatter, since those two are the whole of what the Molds page shows per mold, and a mold rendering as an empty row is the same silent failure that page exists to end. The registry's `walk` skips `_`-prefixed files, so the frontmatter-coverage checks never see the molds — this is the only check that does.
- **A Where We Are that is logging** — § Where We Are is measured in prose words (`proseWords`: `stripMd`, then any table or list scaffolding off) and held to a **flat 400-word limit**. The section holds the project's stage and what blocks its next step, each blocker naming its tracker ID, with no dates, no past tense, no log and no risks, and the alarm's message leads with that purpose before naming the smells. **A record defect rather than a format one:** the page renders correctly and the record is wrong, which is why the banner's copy names two classes. It makes no judgement about *which* sentences are log lines — that is the four rules' (`CONTRIBUTING.md` → The ROADMAP and the briefing are not changelogs), and unbuildable here, so the message carries the smells in words instead of triggering on them: a past-tense regex fires on legitimate current-state prose, and a date regex fires on present-tense board state and citations. A section holding a stage and its blockers runs well under a hundred words and a fresh shelf a few dozen, so the limit needs no second condition to stay silent on day one. It is **not compared to the queue** — a limit that scales with § What's Next lets a long queue license a long current-state section.
- **The surface self-reports** — any failing invariant renders an amber band on every `/system` page, naming the parser, the source doc, and the problem, and prints a `DRIFT [system-surface] …` line at build time (the token leads so a build log is greppable). **The band's copy names both classes** — a format drift with a hollow page below it, and a record defect on a page that renders fine.
- **Warn at build, gate at `verify`** — two audiences, one alarm. **`next build` warns and passes:** a drifted doc format is a record defect, not a compile error, and it must never block a deploy or catch you mid-edit. **`npm run verify` is the gate** (`scripts/verify.mjs`): it runs the build, reads the alarms back, and exits non-zero listing each one. Run it before you push — `npm run check` covers the code, `verify` covers the record. The split exists because an alarm printed into a build that exits 0 stops nobody: format drift fails silently by nature, so the alarm is the only signal there is.
- **Markers at the source** — each parsed section opens with an HTML comment naming its parser, its page, and the shape that must hold.

A firing alarm means the **doc** drifted from spec — fix the doc, or change spec + parser deliberately, never one without the other.

> **Calibration: presence-not-count.** The invariants are tuned so a fresh project — empty trackers, no archive, no decisions, an empty queue — boots with **zero alarms**. What still fires is real drift: a populated list with a malformed row (a question missing its area, an FC missing its trigger, a doc missing its tier), the Work Model / tiers / glossary shape, the seed↔queued-row bidirectional match, a state doc pointing at a tracker ID that no longer exists (a fresh ROADMAP names none, so this fires nothing until the project has both references and trackers), and the styleguide's own completeness (the starter `globals.css` meets its thresholds: ≥200 tokens, the `SEMANTIC TOKENS — Surface` + `Font Size` sections, ≥1 shared component dir). As the project fills in, the surface stays honest without ever nagging an empty list. A mature repo may want count floors (e.g. "≥N archived phases, ≥N decisions") — adding them back is a deliberate re-tightening, logged in `decisions.md`. **One ships: Where We Are's 400-word limit.** It keeps the calibration's promise without a second condition, because a fresh shelf's Where We Are runs a few dozen words.
